.row.text-center
  - @items.each do |item|
    .col-md-4
      = image_tag item.image_url, width: '250px'
      %h3= item.name
      %p
        = item.price
        円 - 在庫数:#{item.stock}
      %p
        - if current_user
          = link_to '購入', purchase_item_path(item), data: { confirm: '本当に購入しますか？' }, class: 'btn btn-warning'
        = link_to '商品ページへ', item, class: 'btn btn-info'
